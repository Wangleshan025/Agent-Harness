# REFLECTION.md

> HarnessX — Coding Agent Harness 项目反思报告
> 字数要求：1500–2500 字

---

## 一、Superpowers 技能评估

### 哪些技能发挥了最大作用？

brainstorming 技能是项目前期最大的价值来源。它没有直接让我"写一个 Harness"，而是连续追问了十几个问题：你的 Harness 要管理什么领域的 agent？它需要哪些工具？什么样的失败是"可恢复的"？这些问题强迫我把模糊的"做一个 coding agent harness"收敛为"一个能管理 Python 项目测试→修复循环的 CLI 工具"。如果没有这些追问，我很可能直接开始写代码，然后在架构选型上反复摇摆。
subagent-driven-development 在并行 worktree 策略下效率很高。我同时开了 3 个 worktree：一个负责核心循环，一个负责工具层，一个负责测试框架。每个 task 分给一个新鲜 subagent，互不干扰。Day 2 上午到中午，3 个 worktree 各完成了 2-3 个 task，总进度比我线性推进快了一倍。

### 哪些技能"形式大于实质"？

finishing-a-development-branch 技能在本次项目中体验最淡。它要求在每个 worktree 完成后执行一系列检查（测试通过、lint 通过、未提交文件清理等）。这些检查本身有价值，但在一个单人项目中，它们更像是一种"仪式"。我的实际情况是：每个 worktree 只有一个分支、一个 PR、一个 reviewer（我自己），finishing 技能建议的"合并前审查"实际上没有带来额外收益——因为我在开发过程中已经通过 TDD 和即时 review 保证了质量。它让我意识到，Superpowers 的设计目标可能是"团队协作"，在单人项目中有些步骤确实会显得过于正式。

---

## 二、TDD 在 AI 协作下的体验

### TDD 是阻碍还是放大器？

总体来说是放大器，但在某些边缘场景下是阻碍。
它让 AI 生成的代码有了客观的"验收标准"，而不是"看着好像差不多就行"。
但在实现反馈闭环时，我花了一个多小时才把 mock 逻辑写对，而实现代码本身只用了 20 分钟。那一刻我确实觉得"TDD 在拖慢我"。但也许正是这个复杂的 mock 测试暴露了我循环状态管理的设计缺陷（状态未正确传递），如果没有它，这个问题会在集成阶段爆炸——那时更难排查。

---

## 三、Subagent-Driven 工作流体验

### 智能体能自主运行多久而不偏离主题？

每个 subagent 平均能自主推进 15-20 分钟（约 3-5 个迭代回合），然后开始出现偏离迹象。最常见的偏离是：过度设计。比如在实现 ConfigLoader 时，subagent 开始考虑"支持环境变量覆盖、支持多配置文件合并、支持远程配置中心"——而 PLAN 中只要求"从 JSON 文件读取 allowed_tools 和 danger_patterns"。发现偏离后，我会在 dispatch prompt 中显式加上"只实现 PLAN 中列出的功能，不做额外扩展"，这个问题就基本可控。

### Task 颗粒度体验

最优颗粒度是 1 个文件 / 2-3 个函数，且验收标准是"某个测试从红变绿"。例如：

"在 guardrail.ts 中实现 isDangerous() 函数，使 guardrail.test.ts 中的 3 个测试通过" —— 完美颗粒度。

"实现整个 AgentLoop 类并完成所有工具集成" —— 颗粒度过大，subagent 会在中间迷路。

颗粒度太细（"修改一行代码"）反而不合适，因为 dispatch 开销（启动 subagent、加载上下文、提交 PR）超过了实际实现时间。

---

## 四、SPEC / PLAN 质量的影响

### 规约不清导致的具体案例

冷启动验证发现的 7 个问题中，最让我印象深刻的是 Task 7（edit_file 工具实现）。一个与主开发不同的 agent（我用的是 Gemini CLI）在只读 SPEC+PLAN 的情况下，实现了 edit_file 但缺少路径安全检查——它允许修改项目目录外的文件（如 /etc/passwd）。这暴露了 SPEC 的缺陷：我写了"工具要能读写文件"，但没有明确"必须限制在项目根目录内"。这个漏洞的直接后果是：如果 harness 被恶意 prompt 注入，可能删除用户系统文件。我立即在 SPEC 的"安全威胁模型"一节中补充了"路径遍历防护"要求，并在 PLAN 中为 Task 3（工具层）增加了"路径边界检查"子任务。如果我只用主 agent 开发，这些隐含假设永远不会被显式质疑。

---

## 五、Prompt / Context 策略

### 你最有效的策略是什么？为什么有效？

最有效的策略是 "report file"机制：每次 dispatch subagent 时，我要求它在一个固定位置（如 reports/task-<id>.md）输出执行报告，包含：
- 实现了哪些功能（文件+函数名）
- 哪些测试通过了（贴测试输出）
- 遇到了什么问题、如何解决的
- 对 SPEC/PLAN 的疑问（如果有）
这个机制有效是因为：它强迫 subagent 在输出代码之外做一次"自我总结"，而总结过程中它经常会发现自己的实现偏差。在我记录的 12 个 task 中，有 4 次 subagent 在写报告时主动修改了代码（比如发现日志格式不一致、变量命名不清晰）。这相当于让 AI 自己完成了一次"自我 code review"，成本几乎为零。

---

## 六、凭据与分发的工程价值

### 这些要求迫使你想清楚了哪些问题？

加密方案的选择让我学到了：你永远不应该用密码直接加密数据
分发要求（Docker）让我考虑了"用户如何在容器中安全提供 API Key"的问题。我最终选择通过 -e 环境变量传入

---

## 七、如果重做

### 你会改变什么？

更早进行冷启动验证
将配置加载设计为更早的task

---

## 八、对 Superpowers 方法论的批判

### 它假设了什么？这些假设在你的项目里成立吗？

假设一："每个 task 可以被独立实现。" 在本次项目中，这一点部分成立。但主循环和反馈闭环之间存在紧密耦合 如果完全独立开发，集成时会出现"接口不匹配"的返工。
假设二："subagent 是可靠的执行者。" 不成立，但 Superpowers 的设计似乎并不依赖这个假设成立。它通过"每 task 一个新鲜 subagent"（避免状态污染）、"先写测试再实现"（让失败可检测）、"review 后再 merge"（人工把关）来管理 subagent 的不可靠性
---